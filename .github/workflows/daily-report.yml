name: Daily AI Web3 Report

on:
  schedule:
    - cron: '0 6 * * *'
    - cron: '0 7 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-report:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_PAT }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install

      - name: Generate report
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: node bot.js

      - name: Update reports index
        run: |
          ls reports/*.md | xargs -I{} basename {} > /tmp/reports_list.txt
          node -e "
            const fs = require('fs');
            const files = fs.readFileSync('/tmp/reports_list.txt','utf8').trim().split('\n').filter(Boolean);
            fs.writeFileSync('reports/index.json', JSON.stringify(files, null, 2));
            console.log('index.json updated:', files);
          "

      - name: Commit and push
        run: |
          git config user.name "kimberly-WY"
          git config user.email "kimberly-WY@users.noreply.github.com"
          git add reports/
          git diff --cached --quiet && echo "Nothing to commit" && exit 0
          git commit -m "🌌 Daily AI+Web3 report $(date +%Y-%m-%d)"
          git push https://x-access-token:${{ secrets.GH_PAT }}@github.com/kimberly-WY/ai-web3-daily-pulse.git main
